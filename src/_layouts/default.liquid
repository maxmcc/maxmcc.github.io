<!doctype html>
<html lang="{{ site.locale }}">
    <head>
        {% render "head", metadata: site.metadata, title: page.title %}
    </head>
    <body class="bg-slate-50 dark:bg-slate-950">
        <main class="container mx-auto mt-8 lg:mt-16 mb-16">
            <article class="px-4 prose prose-slate dark:prose-invert mx-auto">
                {{ content }}
            </article>
        </main>
    </body>
</html>
